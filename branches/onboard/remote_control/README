How to compile:

Option 1, with qtcreator:

apt-get install qtcreator

launch, open 'remote_control.pro', click run


Option 2, qmake:

apt-get install libqt4-dev

in this directory, run:

qmake && make -j4 && ./remote_control
